package Day32;

import Day32._02_InstanceModifiers.Protected.Person;
import  Day3._01_JavaVariable;
public class Package_Accessor {

    /**   there are 3 ways to reach out to other classes
     * 1-) import package.*; // we can import all the classes from the package
     * 2-) import package.classname; // specific class only
     * 3-) fully qualified name. the long way
     */


    /**  Access modifier
     * 1-) PUBLIC == accessible by any other classes
     * 2-) default (non-specified) == this class is only accesible by classes in the same package
     */


    /**  Modifiers in fields
     * 1-) Public = code is accessible for all classes
     * 2-) Private = the code is only accessible within the declared class
     * 3-) default = the code is only accessible in the same package
     * 4-) protected = the code is accessible in the same package and subclasses
     */




















}
