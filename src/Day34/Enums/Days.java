package Day34.Enums;

 enum Days {

     /**
      if we need to store multiple constant (final) variable we use ENUM
      (like a days of the week), (or planet names)
      **/

     MONDAY,
     TUESDAY,
     WEDNESDAY,
     THURSDAY,
     FRIDAY,
     SATURDAY,
     SUNDAY;

     int x = 5;

     void X() {
         System.out.println(MONDAY);
     }


}
