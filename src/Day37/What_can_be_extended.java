package Day37;

public class What_can_be_extended {

    /**  Extension
     * Default Class = we can extend the default classes in the same packages
     * Private Class = we cannot extend out of its package
     * All the methods and fields are accessible from child class, unless it is private
     * if you are extending a public class which includes private or default fields or methods, we cannot extend those (P., D.)
     * Final classes = cannot be extended at all
     *
     */





}
